PORT=8080
# Telegram bot token (from @BotFather). Required for real initData validation
# and for Telegram Stars purchases (createInvoiceLink / successful_payment webhook).
# Leave unset for local dev: the app falls back to `debug:<telegramId>:<name>`
# initData and grants a free 1000-Star starter balance.
BOT_TOKEN=
